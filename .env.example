# Must be an existing absolute directory. The agent may only access this directory.
PI_WEB_WORKSPACE=D:\\Program\\agent\\pi\\pi-web-ui

# Optional. Keep false unless you explicitly trust the workspace and need model-driven writes.
PI_WEB_ALLOW_WRITES=false

# Optional listening port. The development and production scripts listen on all network interfaces.
PI_WEB_PORT=30142
